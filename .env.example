# Order Tracker — Environment Variables
#
# These are already set in docker-compose.yml, so you don't need a .env file
# for Docker. This file is just for reference / running outside Docker.

# Port the server listens on
PORT=3001

# Path to the SQLite database file
DB_PATH=./data/orders.db

# Set to "production" to serve the built React frontend
NODE_ENV=production
